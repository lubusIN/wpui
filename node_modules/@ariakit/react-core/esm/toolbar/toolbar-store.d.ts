import * as Core from "@ariakit/core/toolbar/toolbar-store";
import type { CompositeStoreFunctions, CompositeStoreOptions, CompositeStoreState } from "../composite/composite-store.js";
import type { Store } from "../utils/store.js";
export declare function useToolbarStoreOptions(props: ToolbarStoreProps): Partial<import("../collection/collection-store.js").CollectionStoreOptions<{
    id: string;
    element?: HTMLElement | null | undefined;
} & {
    rowId?: string | undefined;
    disabled?: boolean | undefined;
    children?: string | undefined;
}>>;
export declare function useToolbarStoreProps<T extends ToolbarStore>(store: T, props: ToolbarStoreProps): T;
/**
 * Creates a toolbar store.
 * @see https://ariakit.org/components/toolbar
 * @example
 * ```jsx
 * const toolbar = useToolbarStore();
 * <Toolbar store={toolbar}>
 *   <ToolbarItem>Item 1</ToolbarItem>
 *   <ToolbarItem>Item 2</ToolbarItem>
 *   <ToolbarItem>Item 3</ToolbarItem>
 * </Toolbar>
 * ```
 */
export declare function useToolbarStore(props?: ToolbarStoreProps): ToolbarStore;
export interface ToolbarStoreState extends Core.ToolbarStoreState, CompositeStoreState {
}
export interface ToolbarStoreFunctions extends Core.ToolbarStoreFunctions, CompositeStoreFunctions {
}
export interface ToolbarStoreOptions extends Core.ToolbarStoreOptions, CompositeStoreOptions {
}
export type ToolbarStoreProps = ToolbarStoreOptions & Core.ToolbarStoreProps;
export type ToolbarStore = ToolbarStoreFunctions & Store<Core.ToolbarStore>;
