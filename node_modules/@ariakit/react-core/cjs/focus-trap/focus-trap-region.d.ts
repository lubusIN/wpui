import type { As, Options, Props } from "../utils/types.js";
/**
 * Returns props to create a `FocusTrapRegion` component.
 * @see https://ariakit.org/components/focus-trap-region
 * @example
 * ```jsx
 * const props = useFocusTrapRegion();
 * <Role {...props} />
 * ```
 */
export declare const useFocusTrapRegion: import("../utils/types.js").Hook<FocusTrapRegionOptions<"div">>;
/**
 * Renders a focus trap region element.
 * @see https://ariakit.org/components/focus-trap-region
 * @example
 * ```jsx
 * <FocusTrapRegion>
 *  <Button>click me</Button>
 *  <Button>trap focus</Button>
 *  <Button disabled>disabled Button</Button>
 * </FocusTrapRegion>
 * ```
 */
export declare const FocusTrapRegion: import("../utils/types.js").Component<FocusTrapRegionOptions<"div">>;
export interface FocusTrapRegionOptions<T extends As = "div"> extends Options<T> {
    /**
     * If true, it will trap the focus in the region.
     * @default false
     */
    enabled?: boolean;
}
export type FocusTrapRegionProps<T extends As = "div"> = Props<FocusTrapRegionOptions<T>>;
