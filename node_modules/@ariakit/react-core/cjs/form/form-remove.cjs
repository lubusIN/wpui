"use strict";Object.defineProperty(exports, "__esModule", {value: true});

var _OTRABJ4Ycjs = require('../__chunks/OTRABJ4Y.cjs');


var _NXDHPYNQcjs = require('../__chunks/NXDHPYNQ.cjs');
require('../__chunks/OSQTQ6OF.cjs');
require('../__chunks/I4JDDV3V.cjs');
require('../__chunks/LHHW5ZQP.cjs');




var _CWY2PHPRcjs = require('../__chunks/CWY2PHPR.cjs');


var _5WLTMVVQcjs = require('../__chunks/5WLTMVVQ.cjs');
require('../__chunks/7WUTS26Y.cjs');




var _72I2GWXFcjs = require('../__chunks/72I2GWXF.cjs');

// src/form/form-remove.ts
var _react = require('react');
var _dom = require('@ariakit/core/utils/dom');
var _misc = require('@ariakit/core/utils/misc');
function findNextOrPreviousField(items, name, index) {
  const fields = items == null ? void 0 : items.filter(
    (item) => item.type === "field" && item.name.startsWith(name)
  );
  const regex = new RegExp(`^${name}\\.(\\d+)`);
  const nextField = fields == null ? void 0 : fields.find((field) => {
    const fieldIndex = field.name.replace(regex, "$1");
    return parseInt(fieldIndex) > index;
  });
  if (nextField)
    return nextField;
  return fields == null ? void 0 : fields.reverse().find((field) => {
    const fieldIndex = field.name.replace(regex, "$1");
    return parseInt(fieldIndex) < index;
  });
}
function findPushButton(items, name) {
  return items == null ? void 0 : items.find((item) => item.type === "button" && item.name === name);
}
var useFormRemove = _CWY2PHPRcjs.createHook.call(void 0, 
  (_a) => {
    var _b = _a, { store, name: nameProp, index, autoFocusOnClick = true } = _b, props = _72I2GWXFcjs.__objRest.call(void 0, _b, ["store", "name", "index", "autoFocusOnClick"]);
    const context = _react.useContext.call(void 0, _OTRABJ4Ycjs.FormContext);
    store = store || context;
    _misc.invariant.call(void 0, 
      store,
      process.env.NODE_ENV !== "production" && "FormRemove must be wrapped in a Form component"
    );
    const name = `${nameProp}`;
    const onClickProp = props.onClick;
    const onClick = _5WLTMVVQcjs.useEvent.call(void 0, (event) => {
      var _a2;
      onClickProp == null ? void 0 : onClickProp(event);
      if (event.defaultPrevented)
        return;
      if (!store)
        return;
      store.removeValue(name, index);
      if (!autoFocusOnClick)
        return;
      const { items } = store.getState();
      const item = findNextOrPreviousField(items, name, index);
      const element = item == null ? void 0 : item.element;
      if (element) {
        element.focus();
        if (_dom.isTextField.call(void 0, element)) {
          element.select();
        }
      } else {
        const pushButton = findPushButton(items, name);
        (_a2 = pushButton == null ? void 0 : pushButton.element) == null ? void 0 : _a2.focus();
      }
    });
    props = _72I2GWXFcjs.__spreadProps.call(void 0, _72I2GWXFcjs.__spreadValues.call(void 0, {}, props), {
      onClick
    });
    props = _NXDHPYNQcjs.useButton.call(void 0, props);
    return props;
  }
);
var FormRemove = _CWY2PHPRcjs.createComponent.call(void 0, (props) => {
  const htmlProps = useFormRemove(props);
  return _CWY2PHPRcjs.createElement.call(void 0, "button", htmlProps);
});
if (process.env.NODE_ENV !== "production") {
  FormRemove.displayName = "FormRemove";
}



exports.FormRemove = FormRemove; exports.useFormRemove = useFormRemove;
